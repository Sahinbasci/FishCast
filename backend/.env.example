# FishCast Backend Environment Variables
# Copy to .env for local development

# --- Required for production ---
# GCP project auto-provides credentials via Cloud Run service account
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json

# --- Optional ---
# Stormglass API key for sea temp/wave data (free tier: 10 req/day)
# Without this, monthly average fallback is used
STORMGLASS_API_KEY=

# CORS origins — comma-separated, no spaces around commas.
# Default (when unset): http://localhost:3000-3002 + http://127.0.0.1:3000-3002 + https://fishcast.app + https://www.fishcast.app
# Example:
# CORS_ALLOWED_ORIGINS=https://fishcast-app.vercel.app,https://fishcast.app,http://localhost:3000

# Trace guard: set to "true" only in dev/staging.
# When "false" (default), traceLevel=full requests are downgraded to minimal.
ALLOW_TRACE_FULL=false

# Offline mode: skip ALL external API calls and return fixture data.
# WARNING: never set to "true" in production.
OFFLINE_MODE=false

# Git commit SHA — set by CI/CD pipeline automatically.
# Exposed via GET /_meta for build tracing and deploy verification.
# GIT_SHA=

# NOTE: Report timestamps are stored as native Firestore Timestamps (not ISO strings).
# API responses serialize timestamps to ISO 8601 format.
